# Machine overlays

Optional repository-managed machine overlays live here as `<hostname>.nuon`.
They are merged after `common`, the OS overlay, and the selected role/profile.
For machine-private overrides use `~/.config/dotfiles/machine-overlay.nuon`, which is never synchronized.
